const Loading = () => (
  <div className="flex align-center justify-center h-screen w-screen items-center">
    <img src="./loading.gif" className="h-10" alt="Loading" />
  </div>
);

export default Loading;
